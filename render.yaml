services:
  - type: web
    name: mlbag-proxy
    runtime: python
    buildCommand: echo "no build needed"
    startCommand: python3 server.py
    envVars:
      - key: PORT
        generateValue: false
